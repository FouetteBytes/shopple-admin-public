import AdminLogin from '@/components/auth/AdminLogin';

export default function AdminLoginPage() {
  return <AdminLogin />;
}

export const metadata = {
  title: 'Admin Login - Product Classifier Dashboard',
  description: 'Secure admin login for the product classifier dashboard',
};
